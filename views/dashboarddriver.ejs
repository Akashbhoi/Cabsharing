
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" type="text/css" href="css/style.css" />

    <title>Landing Page Design Using HTML & CSS</title>
  </head>
  <body>
    <div class="background_img-1">
      <div class="header">
        <a href="/dashboarddriver"
          ><img src="css/images/Cab-Sharing.svg" alt="" class="logo"
        /></a>
        <nav>
          <ul>
            <a href ="/driverdetail"><button class="btn" id="btn2" style="padding: 15px 100px">Driver Details</button></a>
          </ul>
        </nav>
        <a href="/profiledriver"><button class="btn" id="btn2">Profile</button></a>
        <a href="/logout"><button class="btn" id="btn1">Log out</button></a>
      </div>
    </div>
    <div class="request-list">
      <% for(const journeyData of journeyDataArr){%>
      <ul>
        <% for(const requester of journeyData.pending){ %>
        <li>
          <%=requester%> has requested you for share the cab for
          <%=journeyData.origin%> to <%=journeyData.destination%>
          <form action="/dashboarddriver" method="post">
            <input
              type="hidden"
              name="journeyID"
              value="<%=journeyData._id%>"
            />
            <input type="hidden" name="Noof" value="<%=journeyData.Noof%>" />
            <button type="submit" name="response" value="accept">ACCEPT</button>
            <button type="submit" name="response" value="reject">REJECT</button>
          </form>
        </li>
        <% } %>
      </ul>
      <% } %>
    </div>
  </body>
</html>
